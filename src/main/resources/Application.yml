server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/gestionConge # Utilisez le port par d�faut MySQL (3306) et ajustez l'URL
    username: root
    password: Root

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      dialect: org.hibernate.dialect.MySQLDialect  # Utilisez le dialecte MySQL
      show_sql: true
  main:
    web-application-type: servlet
